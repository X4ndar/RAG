name: rag-saas

# Production override stub. Apply with:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# Tighten this before any real deploy: remove published Postgres/Qdrant/Redis
# ports, front the backend and frontend behind Caddy or Nginx with TLS, and
# move secrets into a proper secret store (Docker secrets, Vault, etc).

services:
  postgres:
    ports: []
    restart: always

  qdrant:
    ports: []
    restart: always

  redis:
    ports: []
    restart: always

  backend:
    restart: always
    environment:
      APP_ENV: production
    # Explicitly nuke the dev bind mount from docker-compose.yml. Prod must
    # run only the code that was COPY'd into the image at build time.
    # The !reset tag is the Compose spec's clear-list directive — `volumes: []`
    # alone is merged additively with the base and leaves the bind mount intact.
    volumes: !reset []
    # ports: []  # uncomment once a reverse proxy is in front

  frontend:
    restart: always
    # ports: []  # uncomment once a reverse proxy is in front

  # caddy:
  #   image: caddy:2-alpine
  #   restart: always
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
  #     - caddy_data:/data
  #     - caddy_config:/config
  #   networks: [ragnet]

# volumes:
#   caddy_data:
#   caddy_config:
